import { Meta } from '@storybook/react'

import { TableCards } from '.'

export default {
  component: TableCards,
  title: 'Components/Table',
} as Meta<typeof TableCards>

export const Default = {}
